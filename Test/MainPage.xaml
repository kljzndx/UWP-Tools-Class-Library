<Page
    x:Class="Test.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Test"
    xmlns:pctol="using:HappyStudio.UwpToolsLibrary.Control"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <Button x:Name="ScanFolder_Button" Content="文件夹扫描" Click="ScanFolder_Button_Click"/>
            <TextBlock Grid.Column="1">
                <TextBlock.Inlines>
                    <Run Text="项目数量:"/>
                    <Run Text="{x:Bind _strs.Count, Mode=OneWay}"/>
                </TextBlock.Inlines>
            </TextBlock>
        </Grid>
        <ListView Grid.Row="1" ItemsSource="{x:Bind _strs}"/>
    </Grid>
</Page>
