<Page
    x:Class="Test.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Test"
    xmlns:pctol="using:HappyStudio.UwpToolsLibrary.Control"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <pctol:FrostedGlassBackground x:Name="MyGlassBackground" GlassBackgroundBrush="{ThemeResource SystemControlBackgroundAccentBrush}" GlassOpacity="0"/>

        <Border Width="210" Padding="5" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Border.Background>
                <SolidColorBrush Color="White" Opacity="0.4"/>
            </Border.Background>
            <Slider Header="毛玻璃不透明度" Value="{x:Bind MyGlassBackground.GlassOpacity, Mode=TwoWay}" Maximum="1" StepFrequency="0.01" SmallChange="0.01"/>
        </Border>
    </Grid>
</Page>
